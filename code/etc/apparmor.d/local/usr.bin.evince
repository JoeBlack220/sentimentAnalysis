# Site-specific additions and overrides for usr.sbin.cupsd.
# For more details, please see /etc/apparmor.d/local/README.

# Enable user name mapping so printing works 
  /opt/quest/lib64/nss/libnss_vas4.so.2 mr,
  /run/user/*/gvfs-metadata/* lr,
  /var/opt/quest/vas/vasd/.vasd40_ipc_sock rw,
  /etc/opt/quest/vas/vasipc_timeout r,

# Allow evince over 'ssh -X'
  /bin/dbus-daemon Uxr,
